<?xml version="1.0" encoding="utf-8"?>

<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- SHADOW LAYER -->
    <item android:left="@dimen/rounded_button_shadow_size"
          android:top="@dimen/rounded_button_shadow_size">
        <shape>
            <solid android:color="@color/button_background_shape_disabled_color" />
            <corners android:radius="@dimen/rounded_border_radius"/>
        </shape>
    </item>
    <!-- CONTENT LAYER -->
    <item android:bottom="@dimen/rounded_button_shadow_size"
          android:right="@dimen/rounded_button_shadow_size">
        <shape>
            <solid android:color="@color/button_background_disabled_color" />
            <corners android:radius="@dimen/rounded_border_radius"/>
        </shape>
    </item>
</layer-list>